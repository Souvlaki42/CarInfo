generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = env("DATABASE_URL")
  relationMode = "prisma"
}

model Car {
  id String @id @default(uuid()) // cuid
  engineNumber String
  frame String
  year String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}